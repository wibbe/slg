
SET(SOURCE
  Main.cpp
)

SET(LIBRARIES
  ${OPENGL_LIBRARIES}
  ${EXTRA_LIBRARIES}
  ${GLFW_LIBRARIES}
  slg
)

ADD_EXECUTABLE(example ${SOURCE})
TARGET_LINK_LIBRARIES(example ${LIBRARIES})

# Installation
INSTALL(TARGETS example CONFIGURATIONS Release DESTINATION bin)